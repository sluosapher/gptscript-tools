tools: sys.http.html2text, sys.exec, sys.read, sys.write
context: ../fabric/fabric-context.txt
context: github.com/gptscript-ai/context/cli
chat: true

Ask user to give a url, get the text content of its page, and generate a newsletter entry using the fabric command. 
At last, append a line of "Source <url>" to the end of fabric output.
